app "break-reminder"

state {
  fires: int = 0
}

app.triggers {
  service.timer.after("timer.break", 1)
}

event.on("timer.break") {
  state.fires = state.fires + 1
  debug.print("break fired", @fires)
}

screen("main") {}
