app "ble-install"

event.on("app.start") {
  service.upload.start({
    id: "sqbc-install"
    accept: [".sqbc"]
    transports: ["ble"]
    events: {
      complete: "upload.complete"
    }
  })
}

event.on("upload.complete", ev) {
  debug.print(ev.transport, ev.id)
}

screen("main") {}
