app "state-counter"

state {
  count: int = 0
}

event.on("app.start") {
  state.load()
  debug.print("count", state.count)
}

event.on("key.SELECT") {
  state.load()
  state.count = state.count + 1
  state.save()
  debug.print("count", state.count)
}

screen("main") {
}
