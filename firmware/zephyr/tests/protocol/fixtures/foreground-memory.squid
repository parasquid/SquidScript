app "foreground-memory"

state {
  count: int = 0
}

event.on("app.start") {
  state.count = state.count + 1
  debug.print("memory start", state.count)
}

event.on("key.SELECT") {
  state.count = state.count + 1
  debug.print("memory select", state.count)
}

screen("main") {
}
