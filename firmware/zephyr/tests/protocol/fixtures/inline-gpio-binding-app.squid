app "inline-gpio-binding-app" target "esp32c3-super-mini"

device {
  indicator {
    use "gpio:GPIO8"
  }
}

event.on("app.start") {
  service.indicator.write(true)
  debug.print("inline binding ready")
}

screen("main") {
}
