app "device-binding-app" target "esp32c3-super-mini"

device {
  indicator { use "device/indicator.sqdevice" }
}

event.on("app.start") {
  service.indicator.write(true)
  debug.print("binding ready")
}

screen("main") {}
