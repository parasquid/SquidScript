screen("browser") {
  service.display.clear("white")
  drawTopBar("BinBook Reader")

  if (browserIndex == 0) {
    service.display.rect(20, 120, 440, 72, { fillColor: "gray4" })
  }

  service.display.text("Browse for BinBook", { x: 36, y: 142, w: 408, h: 32, fontHeight: 24 })

  if (file != "") {
    if (browserIndex == 1) {
      service.display.rect(20, 220, 440, 112, { fillColor: "gray4" })
    }

    service.display.text("Resume", { x: 36, y: 238, w: 408, h: 32, fontHeight: 24 })
    service.display.text(title, { x: 36, y: 278, w: 320, h: 28, fontHeight: 18 })
    service.display.text(string.format("{}/{}", pageIndex + 1, pageCount), {
      x: 360,
      y: 278,
      w: 84,
      h: 28,
      fontHeight: 18,
      align: "right"
    })
  } else {
    service.display.text("No book to resume", { x: 36, y: 238, w: 408, h: 32, fontHeight: 18 })
  }

  drawBottomBar("UP/DOWN select  SELECT open  BACK exit")
}
