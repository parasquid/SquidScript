[package]
name = "squid-device-protocol"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"

[features]
default = ["std"]
std = ["alloc"]
alloc = []

[dependencies]
crc32fast = { version = "1", default-features = false }
squidvm-limits = { path = "../squidvm-limits" }
