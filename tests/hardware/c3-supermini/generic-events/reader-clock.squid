app "reader-clock"

event.on("app.start") {
  debug.print("reader start")
  event.addSource("timer.clock", { every: 500 })
}

event.on("timer.clock") {
  debug.print("reader clock")
}

screen("main") {}
