app "main"

state {
  starts: int = 0
}

event.on("app.start") {
  state.starts = state.starts + 1
  debug.print("main start", state.starts)
  app.arm("break-reminder")
  app.launch("reader-clock")
}
